<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %> </title>
</head>
<body>
    <h1> Hora da ultima atualização <%= hours %>:<%= minutes %>:<%= seconds %>  </h1>
    <% if(seconds % 2 == 0) { %>
        <h1>Par </h1>
        <% } else { %>
            <h1>Impar</h1>
        <% } %>

    <% items.forEach((marca) => { %>
       <p><%= marca %></p> 
    <% }); %>

    <%- include("./footer.ejs") %>
</body>
</html>